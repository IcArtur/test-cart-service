[tool.ruff]
line-length = 140
target-version = "py311"
extend-exclude = ["migrations", "venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.ruff.lint.isort]
combine-as-imports = true
known-first-party = ["config", "orders"]
